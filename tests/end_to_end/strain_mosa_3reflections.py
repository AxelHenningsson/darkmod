import numpy as np
import matplotlib.pyplot as plt 

if __name__=='__main__':
    # TODO: 
    # 0. Compute 3 reflections that can be probed.
    #    use 3 that come at eta=0 with different omega
    #    let us keep the base cradle tilt, mu=0. The probed
    #    volume is then always the same.
    # 1. Strain mosa for each.
    # 2. Compute com in 3D for each.
    # 3. Convert to Q-vectors for each.
    # 4. Compute estimated F per voxel
    # 5. Compare to input F field.